# pnpm configuration
shamefully-hoist=true

# Skip Puppeteer browser download (browsers installed separately or not needed)
PUPPETEER_SKIP_DOWNLOAD=true

# Use strict peer dependencies
strict-peer-dependencies=false
